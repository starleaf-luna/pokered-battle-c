#include "stats.h"

/*
    Gen I formula:

    HP:    ([Base + DV] * 2 + [sqrt(statexp) / 4] * Level / 100) + Level + 10
    Other: ([Base + DV] * 2 + [sqrt(statexp) / 4] * Level / 100) + 5
*/
    
unsigned char calculate_atk_stat(unsigned char mon_index, unsigned char mon_level) {
    // todo
}
